#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a,b ;
    a=42;
    b=42;
    printf("a + b = %d\n ",a + b);
    printf("a - b = %d\n ",a - b);
    printf("a * b = %d\n ",a * b);
    printf("a / b = %d\n ",a / b);
    return 0;
}
